import {type StructuredTool } from '@langchain/core/tools';

// Here we'll define any custom tools needed for the MCP, 
// but for the basic setup, we'll use the tools from the AgentKit

export const tools: StructuredTool[] = [];